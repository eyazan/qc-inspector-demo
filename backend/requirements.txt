# QC Inspector backend — core runtime (runs on the local MacBook, CPU).
# Pinned to versions verified on Python 3.14 / macOS arm64.

fastapi==0.137.2
uvicorn[standard]==0.49.0
pydantic==2.13.4
pydantic-settings==2.14.2
python-multipart==0.0.32

httpx==0.28.1

PyMuPDF==1.27.2.3
pillow==12.2.0
numpy==2.4.6

SQLAlchemy==2.0.51

reportlab==5.0.0
rapidfuzz==3.14.5

# Model acquisition (scripts/download_models.py)
huggingface_hub==1.20.1

# ---------------------------------------------------------------------------
# Layout detection (PP-DocLayoutV3) — LOCAL provider, see requirements-ml.txt.
# Kept out of the core set so the API + mock providers install cleanly without
# the heavy ML stack. Install separately on the machine that runs layout:
#     pip install -r requirements-ml.txt
# OCR (PaddleOCR-VL) and the LLM (Qwen) are REMOTE per the deployment topology
# and need no local packages here.
# ---------------------------------------------------------------------------
